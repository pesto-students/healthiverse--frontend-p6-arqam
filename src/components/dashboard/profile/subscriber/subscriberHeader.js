import ProfileHeader from "../header";

const SubscriberHeader = ({ subscriberProfileData }) => {
    return (
        <>
            <ProfileHeader item={subscriberProfileData} />
        </>
    )
}

export default SubscriberHeader;